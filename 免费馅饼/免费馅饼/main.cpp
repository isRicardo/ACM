#include<iostream>
using namespace std;
#define cap 100001

int main()
{
    int n = 0;
    int x[cap];
    int t[cap];

    scanf("%d",&n);
    while (n)
    {
        memset(x, 0, sizeof(int) * n);
        memset(t, 0, sizeof(int) * n);

        for (int i = 0; i < n; i++)
        {
            scanf("%d%d", &x[i], &t[i]);
        }
        for (int i = 0; i < n; i++)
        {
            if
        }

    }

}